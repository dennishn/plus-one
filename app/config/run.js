(function() {
	'use strict';

	/**
	 * @name ng-demo
	 * @description
	 *
	 * Run phase of the Application.
	 */
	angular
		.module('plus-one')
		.run(run);

	function run($state, $rootScope) {

	}
})();
